apply plugin: 'application'

mainClassName = 'io.purplejs.boot.MainApp'

dependencies {
    compile project( ':purplejs-boot' )
}

run {
    systemProperties['io.purplejs.runMode'] = 'dev';
    systemProperties['io.purplejs.devSourceDirs'] = file('src/main/resources')
}
